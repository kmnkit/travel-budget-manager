# Travel Expense Tracker - Web Application

React + TypeScript + Vite で構築されたWebアプリケーション。

## 🚀 開発環境のセットアップ

### 前提条件

- Node.js v22 以上
- npm v10 以上
- Supabase プロジェクトの作成完了

### インストール

```bash
cd web
npm install
```

### 環境変数の設定

1. ルートディレクトリの `.env.example` を参考に、`web/.env` ファイルを作成
2. Supabase の Project URL と Anon Key を設定

```env
VITE_SUPABASE_URL=your-project-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 開発サーバーの起動

```bash
npm run dev
```

ブラウザで http://localhost:5173 を開きます。

### ビルド

```bash
npm run build
```

ビルド成果物は `dist/` ディレクトリに生成されます。

### プレビュー

```bash
npm run preview
```

## 📁 プロジェクト構成

```
web/
├── src/
│   ├── components/    # 再利用可能なUIコンポーネント
│   ├── pages/         # ページコンポーネント
│   ├── hooks/         # カスタムReactフック
│   ├── contexts/      # React Context
│   ├── lib/           # ライブラリ設定（Supabaseなど）
│   ├── types/         # TypeScript型定義
│   ├── utils/         # ユーティリティ関数
│   ├── App.tsx        # メインアプリケーション
│   ├── main.tsx       # エントリーポイント
│   └── index.css      # グローバルスタイル
├── public/            # 静的ファイル
└── dist/              # ビルド成果物
```

## 🛠️ 技術スタック

- **React**: 19.2.0
- **TypeScript**: 5.9.3
- **Vite**: 7.2.4
- **Tailwind CSS**: 4.0+ (with @tailwindcss/postcss)
- **React Router**: 6.x
- **Supabase**: JavaScript Client
- **状態管理**: Zustand
- **フォーム**: React Hook Form + Zod
- **チャート**: Recharts
- **通知**: React Hot Toast
- **日付処理**: date-fns

## 🎨 カラーテーマ

Tailwind CSS で以下のカスタムカラーを定義:

- **primary**: #E63946 (メインの赤)
- **primary-dark**: #C1121F (濃い赤)
- **primary-light**: #F8B4B4 (薄い赤)
- **accent**: #FCA311 (アクセントカラー)
- **neutral**: #457B9D (ニュートラルカラー)
- **neutral-dark**: #1D3557 (濃いニュートラル)
- **neutral-light**: #F1FAEE (薄いニュートラル)

## 📝 開発ガイドライン

詳細な開発ガイドラインは [CLAUDE.md](../CLAUDE.md) を参照してください。
