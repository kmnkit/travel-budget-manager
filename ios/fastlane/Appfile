# Appfile for TripWallet
# For more information about the Appfile, see:
#     https://docs.fastlane.tools/advanced/#appfile

# App Bundle Identifier
app_identifier("com.kmnkit.tripWallet")

# Your Apple ID email address
# Replace with your actual Apple ID
apple_id(ENV["APPLE_ID"] || "kmnkit@gmail.com")

# App Store Connect Team ID (numeric)
# Find at: App Store Connect > Users and Access > Team ID
itc_team_id(ENV["ITC_TEAM_ID"] || "1022844458")

# Apple Developer Portal Team ID
# Find at: Xcode > Preferences > Accounts > Team ID
team_id(ENV["TEAM_ID"] || "7W8B42BG5D")

# For CI/CD: Use App Store Connect API Key
# Download from: App Store Connect > Users and Access > Keys
# json_key_file(ENV["APP_STORE_CONNECT_API_KEY_PATH"])

# For a specific lane, you can override values:
# for_lane :beta do
#   app_identifier("com.kmnkit.tripWallet.beta")
# end
